# نظام طلبات التواصل بين المشترين والبائعين

## الميزات الجديدة 🎯

### 1️⃣ إرسال طلب تواصل (للمشتري)
- المشتري يضغط على زر "طلب تواصل" على بطاقة المنتج
- يتم إرسال طلب POST إلى `/requests` يتضمن معرّف المنتج
- الزر يتحول إلى أخضر مع أيقونة صح ✓
- يظهر النص "تم طلب التواصل"
- يحفظ الطلب في حالة "pending" (قيد الانتظار)

### 2️⃣ استقبال الطلبات (للبائع)
- البائع يرى جميع الطلبات المستقبلة على صفحته الشخصية
- قسم جديد "🔔 طلبات من المشترين"
- يعرض معلومات المشتري:
  - الاسم الكامل
  - المدينة
  - رقم الهاتف
  - اسم المنتج

### 3️⃣ قبول/رفض الطلب
- زر أخضر ✓ لـ **قبول** الطلب
- زر أحمر ✗ لـ **رفض** الطلب

**عند قبول الطلب:**
- يتم تحديث حالة الطلب إلى "accepted" (مقبول)
- يتم إنشاء رسالة تُرسل للمشتري تتضمن:
  - رقم هاتف البائع
  - تأكيد قبول الطلب

**عند رفض الطلب:**
- يتم تحديث حالة الطلب إلى "rejected" (مرفوض)
- لا يتم إرسال أي رسالة للمشتري

## الجداول المستخدمة 🗄️

### جدول `req` (الطلبات)
```
- req_id: معرّف فريد للطلب
- user_id: معرّف المشتري
- used_item_id: معرّف المنتج
- status: حالة الطلب (pending, accepted, rejected)
- comment: تعليق المشتري (اختياري)
- rating: تقييم المشتري (اختياري)
- seller_message: رسالة البائع (للإرسال للمشتري)
- createdAt: وقت إنشاء الطلب
- updatedAt: وقت آخر تحديث
```

### جدول `message` (الرسائل)
```
- msg_id: معرّف فريد للرسالة
- sender_id: معرّف المُرسل
- receiver_id: معرّف المُستقبِل
- req_id: معرّف الطلب المرتبط
- content: محتوى الرسالة
- sender_phone: رقم هاتف المُرسل
- is_read: هل تمت قراءة الرسالة
- createdAt: وقت إرسال الرسالة
```

## المسارات الجديدة (API Routes) 🛣️

### POST `/requests` - إرسال طلب تواصل
**المطلوب:** Token (مصادقة مطلوبة)
**Body:**
```json
{
  "used_item_id": 1
}
```

### GET `/requests/my` - جلب طلباتي (المشتري)
**المطلوب:** Token
**الرد:** قائمة بجميع الطلبات التي أرسلتها

### GET `/requests/received` - جلب الطلبات المستقبلة (البائع)
**المطلوب:** Token
**الرد:** قائمة بجميع الطلبات على منتجاتي

### PUT `/requests/:req_id/accept` - قبول طلب
**المطلوب:** Token + معرّف الطلب
**الرد:** تأكيد القبول + معلومات الطلب

### PUT `/requests/:req_id/reject` - رفض طلب
**المطلوب:** Token + معرّف الطلب
**الرد:** تأكيد الرفض

## الملفات المعدّلة ✏️

### Backend
- ✅ `backend/models/request.js` - إضافة `status` و `seller_message`
- ✅ `backend/models/message.js` - نموذج جديد للرسائل
- ✅ `backend/models/useditem.js` - إضافة ارتباط مع الطلبات
- ✅ `backend/controllers/reqsController.js` - حديث مع accept/reject
- ✅ `backend/routes/ReqsRoutes.js` - مسارات جديدة

### Frontend
- ✅ `frontend/src/components/postItem.js` - زر "طلب تواصل" متفاعل
- ✅ `frontend/src/pages/ProfilePage.js` - عرض الطلبات المستقبلة

## كيفية الاستخدام 🚀

### للمشتري:
1. انقر على أي منتج
2. اضغط على "طلب تواصل"
3. سيتحول الزر إلى أخضر
4. عندما يقبل البائع، ستظهر رسالة برقم هاتفه

### للبائع:
1. اذهب إلى صفحتك الشخصية
2. ابحث عن قسم "🔔 طلبات من المشترين"
3. راجع معلومات المشتري
4. اضغط على ✓ لقبول أو ✗ لرفض
5. عند القبول، سيتم إرسال رقمك للمشتري

## حالات الخطأ ⚠️

- إذا حاول المشتري إرسال طلب مكرر → رسالة "تم إرسال الطلب مسبقاً"
- إذا حاول بائع غير مالك المنتج قبول/رفض الطلب → رسالة "ليس لديك صلاحية"
- إذا لم يكن هناك token → رسالة "يجب تسجيل الدخول"

## ملاحظات مهمة ⚡

- الطلبات تُخزّن في قاعدة البيانات
- يمكن للمشتري رؤية حالة طلبه (قيد الانتظار/مقبول/مرفوض)
- عند قبول طلب، يتم إرسال رسالة تتضمن رقم هاتف البائع
- يمكن إضافة نظام إخطارات في المستقبل
